kind: StagedPolicySequence
apiVersion: api.ibm.com/v1
metadata:
  name: webmPolicy
  namespace: test
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - webMethods
spec:
  transport:
    - $ref: test:SM:1.0
